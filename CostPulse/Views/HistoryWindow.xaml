<Window x:Class="CostPulse.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="History" Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#222">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="History" Foreground="White" FontSize="18" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Filter:" Foreground="#AAA" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="TxtFilter" Width="150" Background="#333" Foreground="White"/>
            <Button Content="Search" Margin="5,0,0,0" Width="60" Click="BtnSearch_Click"/>
            
            <Button Content="Clear All" Margin="20,0,0,0" Width="80" Background="#CC3333" Foreground="White" Click="BtnClearAll_Click"/>
            <Button Content="Export CSV" Margin="10,0,0,0" Width="80" Background="#339933" Foreground="White" Click="BtnExport_Click"/>
        </StackPanel>

        <DataGrid x:Name="GridHistory" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="#333" Foreground="Black" RowBackground="#EEE" AlternatingRowBackground="#DDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="140"/>
                <DataGridTextColumn Header="Provider" Binding="{Binding Provider}" Width="100"/>
                <DataGridTextColumn Header="Model" Binding="{Binding ModelName}" Width="120"/>
                <DataGridTextColumn Header="Input" Binding="{Binding InputTokens}" Width="80"/>
                <DataGridTextColumn Header="Output" Binding="{Binding OutputTokens}" Width="80"/>
                <DataGridTextColumn Header="Cost" Binding="{Binding FormattedCost}" Width="80"/>
                <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="*"/>
                <DataGridTemplateColumn Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="BtnDelete_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
