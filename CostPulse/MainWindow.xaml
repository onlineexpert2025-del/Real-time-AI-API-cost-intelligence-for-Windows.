<Window x:Class="CostPulse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CostPulse"
        mc:Ignorable="d"
        Title="CostPulse Widget" Height="150" Width="250"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    <Border x:Name="MainBorder" Background="#222" CornerRadius="20" BorderThickness="1" BorderBrush="#33FFFFFF" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.4" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Row -->
            <Border Grid.Row="0" Background="Transparent" CornerRadius="20,20,0,0" x:Name="HeaderBorder">
                <Grid x:Name="HeaderGrid" Background="Transparent"> <!-- Using Transparent to hit-test? or just let interactions pass to Border? -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="CostPulse"
                               VerticalAlignment="Center"
                               Margin="15,0,0,0"
                               Foreground="White"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Opacity="0.9"/>

                    <Button Grid.Column="1"
                            Content="✕"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseButton_Click"
                            FontSize="14"/>
                </Grid>
            </Border>

            <!-- Body Content -->
            <Grid Grid.Row="1">
                 <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Today's Spend" Foreground="White" HorizontalAlignment="Center" FontSize="13" Opacity="0.65" Margin="0,0,0,4"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock x:Name="TxtTodayTotal" Text="$0.00" Foreground="White" HorizontalAlignment="Center" FontSize="36" FontWeight="SemiBold"/>
                        <TextBlock x:Name="TxtDelta" Text="▲" Foreground="#44FF44" FontSize="12" Margin="4,8,0,0" Visibility="Collapsed" VerticalAlignment="Top"/>
                    </StackPanel>
                    
                    <TextBlock x:Name="TxtMonthTotal" Text="Month: $0.00" Foreground="White" HorizontalAlignment="Center" FontSize="12" Opacity="0.65" Margin="0,4,0,0"/>
                    
                    <!-- Sparkline (7 Days) -->
                    <!-- Sparkline (7 Days) -->
                    <Canvas x:Name="SparkCanvas" Height="30" Margin="0,8,0,0" Width="210" HorizontalAlignment="Center" ClipToBounds="True" Background="Transparent" />
                    
                    <TextBlock x:Name="TxtTokens" Text="0 tokens" Foreground="White" HorizontalAlignment="Center" FontSize="12" Opacity="0.65" Margin="0,2,0,0"/>
                 </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
